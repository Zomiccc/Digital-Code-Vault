# API base URL the merchant portal should call, including the /api/v1 prefix.
# Leave unset to use a relative path (same-origin deployment or reverse proxy).
# Example: VITE_API_URL=https://api.yourdomain.com/api/v1
VITE_API_URL=
